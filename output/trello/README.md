        # trello MCP Server

        Auto-generated by **MCP-Anything** from OpenAPI spec.

        **API:** Trello REST API v1.0.0  
        **Base URL:** `https://api.trello.com/1`  
        **Tools:** 22 endpoints  

        ## Setup

        1. Copy `.env.example` to `.env` and fill in your credentials:
           ```bash
           cp .env.example .env
           ```

        2. Add to your MCP client config (`mcp_config.json`):
           ```json
           {
  "mcpServers": {
    "trello": {
      "command": "python3",
      "args": [
        "server.py"
      ],
      "env": {
        "TRELLO_BASE_URL": "${TRELLO_BASE_URL}",
        "TRELLO_API_KEY": "${TRELLO_API_KEY}"
      }
    }
  }
}
           ```

        3. Install dependencies:
           ```bash
           pip install fastmcp httpx
           ```

        ## Available Tools

### Boards

| Tool | Method | Path | Description |
|------|--------|------|-------------|
| `getmemberboards` | GET | `/members/{id}/boards` | Get all boards for a member |
| `getboard` | GET | `/boards/{id}` | Get a board by ID |
| `updateboard` | PUT | `/boards/{id}` | Update a board |
| `deleteboard` | DELETE | `/boards/{id}` | Delete a board |
| `createboard` | POST | `/boards` | Create a new board |
| `getboardlists` | GET | `/boards/{id}/lists` | Get all lists on a board |
| `getboardcards` | GET | `/boards/{id}/cards` | Get all cards on a board |
| `createboardlabel` | POST | `/boards/{id}/labels` | Create a label on a board |

### Cards

| Tool | Method | Path | Description |
|------|--------|------|-------------|
| `getboardcards` | GET | `/boards/{id}/cards` | Get all cards on a board |
| `getlistcards` | GET | `/lists/{id}/cards` | Get all cards in a list |
| `getcard` | GET | `/cards/{id}` | Get a card by ID |
| `updatecard` | PUT | `/cards/{id}` | Update a card (rename, move, add labels, set due date, etc.) |
| `deletecard` | DELETE | `/cards/{id}` | Delete a card |
| `createcard` | POST | `/cards` | Create a new card in a list |

### Labels

| Tool | Method | Path | Description |
|------|--------|------|-------------|
| `createboardlabel` | POST | `/boards/{id}/labels` | Create a label on a board |
| `getlabel` | GET | `/labels/{id}` | Get a label by ID |
| `updatelabel` | PUT | `/labels/{id}` | Update a label (rename, change color) |
| `deletelabel` | DELETE | `/labels/{id}` | Delete a label from a board |

### Lists

| Tool | Method | Path | Description |
|------|--------|------|-------------|
| `getboardlists` | GET | `/boards/{id}/lists` | Get all lists on a board |
| `getlist` | GET | `/lists/{id}` | Get a list by ID |
| `updatelist` | PUT | `/lists/{id}` | Update a list (rename, archive, move) |
| `createlist` | POST | `/lists` | Create a new list on a board |
| `getlistcards` | GET | `/lists/{id}/cards` | Get all cards in a list |

### Members

| Tool | Method | Path | Description |
|------|--------|------|-------------|
| `getmymember` | GET | `/members/me` | Get current authenticated member |
| `getmember` | GET | `/members/{id}` | Get a member by ID or username |
| `getmemberboards` | GET | `/members/{id}/boards` | Get all boards for a member |

### Search

| Tool | Method | Path | Description |
|------|--------|------|-------------|
| `search` | GET | `/search` | Search across boards, cards, and members |

